import './style.css'

// Inject the app's HTML markup into the #app container
document.querySelector<HTMLDivElement>('#app')!.innerHTML = `
  <h1>My English</h1>
  <!-- TODO: text → IPA converter goes here (input + button + result) -->
`

